import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CPA Network - Real-time Performance Dashboard",
  description: "Track your CPA campaigns in real-time with clicks, leads, and revenue analytics.",
  keywords: ["CPA", "Affiliate Marketing", "Real-time Dashboard", "Performance Tracking"],
  authors: [{ name: "CPA Network" }],
  openGraph: {
    title: "CPA Network Dashboard",
    description: "Real-time CPA performance tracking dashboard",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
